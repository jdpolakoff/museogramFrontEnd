angular
.module('museoGram', [
  'ui.router',
  'ngResource',
  'wu.masonry',
])


// When running locally
var apiURL = 'http://localhost:3000'

// When running from Heroku
// var apiURL = 'http://localhost:3000'
